#
# reference configuration file
#

qualification:
  os.command.to.get.installed.version: some-command

build:

publish:
  local.artifact.repository.root: ${M2}
#  release.tag: release-${current.version}

install:
  installation.directory: ${RUNTIME_DIR}