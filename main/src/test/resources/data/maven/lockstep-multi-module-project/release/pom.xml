<project>

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.test</groupId>
        <artifactId>maven-root</artifactId>
        <version>88</version>
    </parent>

    <artifactId>release</artifactId>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <finalName>binary-release-A</finalName>
                    <descriptors>
                        <descriptor>src/assembly/release.xml</descriptor>
                    </descriptors>
                </configuration>
                <!--
                    executions go here
                -->
            </plugin>
        </plugins>
    </build>

</project>